export const handleImageError = (brokenImage) => { //Renamed variable
    const container = brokenImage.closest('.portfolio-item'); //Using the renamed variable
    // ... (rest of the code remains the same)
};
